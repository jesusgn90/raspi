
name: TOC

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: TOC Generator
        uses: technote-space/toc-generator@v4.0.0
